name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-tags: true

      - name: Derive version
        id: version
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            tag="$(git tag --list 'v*' --sort=-v:refname | head -1)"
            echo "tag=${tag:-v0.0.0-dev}" >> "$GITHUB_OUTPUT"
            echo "version=${tag#v}" >> "$GITHUB_OUTPUT"
          fi

      - name: Write VERSION
        run: echo "${{ steps.version.outputs.version }}" > VERSION

      - name: Build install script
        run: bash build/build.sh

      - name: Install build deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential dpkg-dev debhelper

      - name: Build deb package
        id: deb_build
        run: bash build/build-deb.sh

      - name: Upload install script
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: sui-control-install.sh
          asset_name: sui-control-install.sh
          tag: ${{ steps.version.outputs.tag }}
          overwrite: true

      - name: Upload deb package
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ../sui-control_${{ steps.deb_build.outputs.version }}_all.deb
          asset_name: sui-control_${{ steps.deb_build.outputs.version }}_all.deb
          tag: ${{ steps.version.outputs.tag }}
          overwrite: true
